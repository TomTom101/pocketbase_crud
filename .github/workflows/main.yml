name: GitHub Actions Demo

# run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on: [push, workflow_dispatch]

jobs:
  build-flutter-web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - run: flutter pub get
      - run: flutter test
      - run: flutter build web --dart-define SSL=false --dart-define PB_HOST=localhost --dart-define PB_PORT=8090